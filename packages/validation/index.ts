// Shared zod validation schemas, used identically on the client (form
// validation) and server (API request validation) per
// docs/09-PLATFORM-ARCHITECTURE.md §8. Scaffolding only — schemas are
// added alongside each API endpoint's implementation in Phase 4.

export {};
