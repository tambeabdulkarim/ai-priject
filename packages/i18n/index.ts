// Shared locale/translation utilities, generalizing the ar/en dictionary
// pattern used by apps/web's frozen Homepage (src/lib/i18n.ts, untouched)
// into a package other apps (admin, mobile) can draw from. Scaffolding
// only — apps/web's own Homepage i18n is intentionally NOT migrated here,
// per the "do not modify the Homepage" constraint.

export {};
