// Shared design-system primitives/patterns per docs/09-PLATFORM-ARCHITECTURE.md
// §3 (primitives/, patterns/, theming/). Scaffolding only — the frozen
// Homepage's own components remain in apps/web/src/components, untouched;
// this package is populated as new apps (admin, mobile) need shared UI.

export {};
