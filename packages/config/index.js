// Shared lint/tsconfig/build configuration for every Phoenix app and package.
// Scaffolding only — populated when Phase 4 (Backend API) begins per
// docs/17-IMPLEMENTATION-ROADMAP.md.
module.exports = {};
