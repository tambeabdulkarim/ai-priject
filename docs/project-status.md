# Phoenix Project Status

*Canonical entry point for all documentation: [`docs/documentation-index.md`](documentation-index.md).*

**Current Phase:** Phase 42 — Platform Completion Audit — **complete. Audit-first phase, not content production: a full, honest assessment of whether Phoenix is genuinely ready for a real learner/instructor/admin to use end to end.** Covered full database integrity (14 direct checks), live RBAC/security testing across all 6 real roles, all 6 target learning paths' content re-verification, resource-library review, API-contract spot-check, deployment-readiness review, frontend route/guard audit, and the full test matrix. **2 real bugs found and fixed, both verified live:** (1) **P1** — a leftover E2E test-fixture course (`Verify Moderator Fix...`) was `status: published` and genuinely visible in the live public course catalog to real visitors; fixed by reverting it to `draft` (not deleted). (2) **P2** — `ProjectsService.evaluateSubmission` checked "already evaluated" before checking real caller authorization, letting an unauthorized user distinguish an evaluated vs. unevaluated submission via 409-vs-403 (no forgery was ever possible, only this status leak); fixed by reordering the checks, with 1 test updated and 1 new regression test added. **No P0 (safety-critical) issue was found anywhere.** **Technical owner decision: Option B — Ready after P0/P1 fixes, and those fixes are already applied and verified.** **Overall Platform Readiness: 91%** — not 100%, honestly, because real deployment still depends on owner-side external-account decisions (unchanged since Phase 17), no frontend unit-test suite exists, and a live responsive/accessibility audit could not be performed this session (no browser-automation tool available). **The 2 path-level architecture gaps from Phase 35 (path-level certificate, path-level capstone) were explicitly re-evaluated and confirmed as real but non-blocking** — every one of the 6 real paths already delivers real, individually-earned course certificates and a real capstone project, and a targeted frontend check confirmed no page ever falsely claims an aggregate path-level version of either exists; decided as POST-LAUNCH / NEXT PHASE, not touched. **Validated for real:** 242/242 backend tests (241 + 1 new), 0 database integrity issues after the fix, 0 RBAC bypasses found across all 6 roles live-tested, both builds clean. Full report: `docs/phase42-platform-completion-audit.md`. See `docs/restore-point-phase42.md`. **Phase 42 complete — awaiting direction on what's next.** Previous phase:

**Phase 41 — Data Scientist Learning Path Production — complete. Executed autonomously end-to-end (no intermediate approval stops). Executed Phase 35's Data Scientist recommendation — the path Phase 35 itself flagged as having the largest genuine gap of all 6 (0 courses reusable in full at the time).** Built the 1 remaining new course (2 of the original 3 "new" courses — Programming Foundations, Database Design & SQL Mastery — had already been built for other paths in Phases 36/37). Added **Data Science Foundations: From Data to Decisions** (5 modules, 18 lessons, 5 quizzes/25 questions, 2 projects) — data cleaning, analyst SQL (GROUP BY/aggregates — confirmed absent from Database Design & SQL Mastery), statistics/probability, hypothesis testing/A-B experimentation, honest visualization/storytelling, and a deliberately light, introductory bridge into predictive modeling and responsible data science. **A real scope decision was made and documented**: `courses.md`'s literal 4-module blueprint had zero predictive-modeling content, but this path's own projects and this phase's own "self-contained end-to-end" objective required at least an introductory bridge — added as a 5th module, explicitly scoped lighter than a future, separate Machine Learning Foundations elective would be. Created the new **`data-scientist` `LearningPath`** (no existing path was a real fit) linking 3 courses (Programming Foundations, Database Design & SQL Mastery, Data Science Foundations) in Phase 35's specified sequence — **3/3 production-ready. This completes all 6 of Phase 35's originally-named learning paths (Frontend Engineer, Backend Engineer, Full Stack Engineer, Cloud Engineer, Cyber Security Analyst, Data Scientist).** **A real, transient 500 error was hit and correctly diagnosed** mid-journey (server stayed healthy, identical retry succeeded cleanly) — consistent with a Neon serverless wake/reconnect blip also seen during this phase's own DB inspection, not a reproducible defect; no code change was made since it did not reproduce. **Validated for real:** idempotent seed (0 duplicates on rerun; all 13 prior content/path seeds re-confirmed idempotent), direct database duplicate-content checks found zero duplicates at every level, 6 new resources live-verified via `WebFetch` (NumPy, pandas, Python statistics module, Seeing Theory, Fundamentals of Data Visualization, scikit-learn — one candidate, Matplotlib, correctly left uncited after a 403), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey across all 3 courses (split into 2 batches to respect the documented quiz-submission rate limit) — using a freshly registered learner account for a genuine from-zero journey: enrolled in each course in turn, completed all lessons, deliberately wrong quiz answers correctly failed with no false-positive completion, correct answers passed all quizzes across all 3 courses, **each course reached 100% and received a brand-new certificate** (3 total, zero duplicates), certificate-issuance idempotency directly re-confirmed via re-trigger, a real capstone data-product project submitted with genuine hypothesis-testing/overfitting/precision-recall/bias reasoning, self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. Course-to-course transition explicitly verified working. Full report: `docs/phase41-data-science-content-production-report.md`. See `docs/restore-point-phase41.md`. **Phase 41 complete — no blocker encountered. All 6 of Phase 35's original named paths are now done. Not starting Phase 42 automatically; awaiting direction on what's next.** Previous phase:

**Phase 40 — Cyber Security Analyst Learning Path Production — complete. Executed autonomously end-to-end (no intermediate approval stops, per this phase's own execution mode). Executed Phase 35's Cyber Security Analyst recommendation — built the 1 new mandatory course, reused 2 already-real courses.** Added **Cyber Security Fundamentals: Defending Modern Systems** (5 modules, 15 lessons, 5 quizzes/25 questions, 2 projects) — a defender-first course: CIA triad, threat modeling, network security, application security (OWASP Top 10 from an analyst's verification lens), cryptography, IAM, and incident response/ethical hacking. Confirmed by direct comparison NOT to duplicate Full-Stack Web Dev Module 6's existing OWASP lesson — that lesson is developer-prevention-focused, this course's Module 3 is analyst-verification-focused, confirmed by directly reading both. Created the new **`cyber-security-analyst` `LearningPath`** (no existing path was a real fit) linking 3 courses (Programming Foundations, Computer Networking Foundations, Cyber Security Fundamentals) in Phase 35's specified sequence — **3/3 production-ready, the platform's fifth fully-realized target path from Phase 35's blueprint.** Phase 35's own "1 new + 2 reused" course-count claim was confirmed accurate, no correction needed. **A real bug was found and fixed autonomously**: 3 stray backticks used for inline-code formatting inside lesson bodies broke the seed file's compile — the same recurring gotcha from Phase 31 — diagnosed and fixed without stopping. **A real, load-bearing rate limit was discovered and correctly handled**: quiz submissions are deliberately capped at 10 requests/15 min per user (confirmed by reading the actual route decorator) — resolved with a genuine 15-minute wait, not a workaround. **Validated for real:** idempotent seed (0 duplicates on rerun; all 12 prior content/path seeds re-confirmed idempotent), direct database duplicate-content checks found zero duplicates at every level, 4 new resources live-verified via `WebFetch` (NIST Cybersecurity Framework, NIST SP 800-61 Rev. 3, MITRE ATT&CK, NIST Crypto Standards — catching and correctly avoiding a withdrawn NIST document along the way), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey across all 3 courses — using a freshly registered learner account for a genuine from-zero journey: enrolled in each course in turn, completed all lessons, deliberately wrong quiz answers correctly failed with no false-positive completion, correct answers passed all quizzes across all 3 courses, **each course reached 100% and received a brand-new certificate** (3 total, zero duplicates), certificate-issuance idempotency directly re-confirmed via re-trigger, a real capstone incident-response project submitted with genuine cryptography/IAM analysis, self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. Course-to-course transition explicitly verified working. Full report: `docs/phase40-cyber-security-analyst-path-production-report.md`. See `docs/restore-point-phase40.md`. **Phase 40 complete — no blocker encountered. Not starting Phase 41 automatically; awaiting direction on what's next (Data Scientist is the one remaining named path from Phase 35's original scope).** Previous phase:

**Phase 39 — Cloud Engineer Learning Path Production — complete. Executed Phase 35's Cloud Engineer recommendation — built the 1 remaining new course, reused 3 already-real courses.** Added **Cloud Computing Foundations: AWS, Azure & GCP** (5 modules, 18 lessons, 5 quizzes/25 questions, 2 projects) — provider-agnostic cloud fundamentals first (IaaS/PaaS/SaaS, shared responsibility), then real compute/storage/networking/IAM/Infrastructure-as-Code/cost-management/architecture-reasoning across AWS, Azure, and GCP. Confirmed by direct comparison NOT to duplicate DevOps Foundations Module 4's existing Terraform/IaC content — the new course's own IaC module explicitly assumes that module as a prerequisite and focuses on cloud-provider-specific application instead. Created the new **`cloud-engineer` `LearningPath`** (no existing path was a real fit) linking 4 courses (Programming Foundations, Computer Networking Foundations, Cloud Computing Foundations, DevOps Foundations) in Phase 35's specified sequence — **4/4 production-ready, the platform's fourth fully-realized target path from Phase 35's blueprint.** Unlike Phase 37, no correction to Phase 35's own course-count claim was needed this time — direct inspection confirmed "2 new + 2 reused" was accurate as written. **Validated for real:** idempotent seed (0 duplicates on rerun; all 11 prior content/path seeds re-confirmed idempotent), direct database duplicate-content checks found zero duplicates at every level, 5 new resources live-verified via `WebFetch` (NIST SP 800-145, AWS Overview whitepaper, Azure docs, GCP docs, AWS Well-Architected Framework), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey across all 4 courses — using a freshly registered learner account for a genuine from-zero journey: enrolled in each course in turn, completed all lessons, deliberately wrong quiz answers correctly failed with no false-positive completion (tested once, course 1), correct answers passed all quizzes across all 4 courses, **each course reached 100% and received a brand-new certificate** (4 total, zero duplicates), certificate-issuance idempotency directly re-confirmed via re-trigger, a real capstone project submitted requiring genuine cloud-architecture reasoning (compute/network/IAM/IaC/reliability/cost, plus an honest 6-pillar Well-Architected self-evaluation with real gaps, not just strengths), self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. Course-to-course transition explicitly verified working. Full report: `docs/phase39-cloud-engineer-path-production-report.md`. See `docs/restore-point-phase39.md`. **Explicitly stopped, awaiting approval before Phase 40.** Previous phase:

**Phase 38 — Full Stack Engineer Learning Path Production — complete. Executed Phase 35's Full Stack Engineer recommendation — but the analysis found ZERO new courses were needed.** Both courses Phase 35 flagged as "new" for this path (Programming Foundations, Database Design & SQL Mastery) were already built for other paths in Phases 36 and 37. Directly re-verified this by inspecting all 5 required courses' real modules/lessons/projects before writing anything — every skill Full Stack Engineer needs, including the one genuinely new "cross-stack integration" skill, is already covered, and a genuine frontend+backend+database-integrated project already exists (Full-Stack Web Dev's "Full-Stack Next.js Feature"). Created the new **`full-stack-engineer` `LearningPath`** (no existing path was a real fit) linking the 5 already-real courses (Programming Foundations, Database Design & SQL Mastery, UI/UX Design Foundations, Full-Stack Web Development with Next.js, DevOps Foundations) in Phase 35's specified sequence — **5/5 production-ready, the platform's third fully-realized target path from Phase 35's blueprint.** This phase's entire content footprint is 1 new database row (`LearningPath`) plus 5 course memberships — zero new modules/lessons/quizzes/questions/projects, an honest "quantity is not the goal" outcome the user's own instructions explicitly authorized. **Validated for real:** idempotent seed (0 duplicates on rerun; all 10 prior content/path seeds re-confirmed idempotent), direct database duplicate-path-slug checks found zero duplicates, 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey covering all 23 requested verification steps across all 5 courses in sequence — using a freshly registered learner account (not the long-lived fixture, which already held certificates for these courses from prior phases) to genuinely exercise a from-zero journey: enrolled in each course in turn, completed all lessons, deliberately wrong quiz answers correctly failed with no false-positive completion (tested once, course 1), correct answers passed all quizzes across all 5 courses, **each course reached 100% and received a brand-new certificate** (5 total, zero duplicates), certificate-issuance idempotency directly re-confirmed via re-trigger, a real project submitted on the genuinely integrated Full-Stack Next.js Feature project, self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. Course-to-course transition explicitly verified working. Full report: `docs/phase38-fullstack-learning-path-production-report.md`. See `docs/restore-point-phase38.md`. **Explicitly stopped, awaiting approval before Phase 39.** Previous phase:

**Phase 37 — Backend Engineer Learning Path Production — complete. Executed Phase 35's Backend Engineer recommendation: built the 1 remaining new course (Database Design & SQL Mastery), reused 4 already-real courses (Programming Foundations, Full-Stack Web Development with Next.js, Computer Networking Foundations, DevOps Foundations), and created the new `backend-engineer` `LearningPath` from scratch — no existing path was a real fit (confirmed by direct inspection).** Added **Database Design & SQL Mastery** (4 modules, 15 lessons, 4 quizzes/20 questions, 2 projects) — teaches relational schema design, SQL querying, normalization, indexing, and transactions from a database-design perspective, confirmed by direct lesson-title comparison NOT to duplicate Full-Stack Web Dev's existing "Databases & ORMs" lesson (which teaches ORM usage at an application-developer level and assumes this course's skills already exist). **Resolved, and disclosed rather than silently fixed, a real counting error in Phase 35's own document**: its Executive Summary said "3 new courses" for Backend Engineer, but its own detailed course list only ever named 2 — confirmed by direct database inspection that no plausible 3rd new course exists; Programming Foundations (Phase 36) was the 1st, Database Design & SQL Mastery is the 2nd and last. **Validated for real:** idempotent seed (0 duplicates on rerun; all 9 prior content/path seeds re-confirmed idempotent), direct database duplicate-content checks found zero duplicates at every level, 1 new resource live-verified via `WebFetch` ("Use The Index, Luke!"), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey covering all 15 requested verification steps — enrolled for real, all 15 lessons completed through the real lesson/quiz flow (wrong answers correctly failed with no false-positive completion, correct answers passed), **completion reached 100%**, a **brand-new certificate issued from zero**, certificate-issuance idempotency directly re-confirmed, a real project submitted, self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. One incidental, pre-existing dev-environment issue found and fixed (a stale `tsconfig.tsbuildinfo` cache silently blocking `dist/` emission) — no source code changed. Full report: `docs/phase37-backend-engineer-content-production-report.md`. See `docs/restore-point-phase37.md`. **Explicitly stopped, awaiting approval before Phase 38.** Previous phase:

**Phase 36 — Frontend Engineer Learning Path Completion — complete. Executed Phase 35's exact recommendation: built the 1 missing course, reused the 2 already-real courses.** Added **Programming Foundations: Problem Solving with Python & JavaScript** (4 modules, 16 lessons, 4 quizzes/20 questions, 2 projects) — scoped deliberately to teach universal, language-agnostic programming concepts primarily through Python, confirmed by direct lesson-title comparison NOT to duplicate Full-Stack Web Dev's existing JavaScript & TypeScript Fundamentals module. Added to the existing `frontend-web` `LearningPath` (not a new path — a disclosed, reasoned reuse decision, since that path already contained exactly the 2 courses Phase 35 identified as reusable) at position 0. **Validated for real:** idempotent seed (0 duplicates on rerun; all 8 prior content/path seeds re-confirmed idempotent), direct database duplicate-content checks found zero duplicates at every level, 3 resources live-verified via `WebFetch` (Python docs reused, Git docs + Python unittest docs new), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey covering all 16 requested verification steps — enrolled for real, all 16 lessons completed through the real lesson/quiz flow (wrong answers correctly failed with a real `QuizAttempt` and no false-positive completion, correct answers passed), **completion reached 100%**, a **brand-new certificate issued from zero**, certificate-issuance idempotency directly re-confirmed, a real project submitted, self-evaluation blocked (403), and real instructor evaluation persisted and re-readable. Full report: `docs/phase36-frontend-engineer-content-production-report.md`. See `docs/restore-point-phase36.md`. **Explicitly stopped, awaiting approval before Phase 37.** Previous phase:

**Phase 35 — Learning Paths Master Blueprint — complete. Planning/analysis only — zero database writes, zero seed runs, zero application/frontend code changes.** Produced a full skill-gap and course-architecture analysis for the 6 currently-empty named learning paths (Backend Engineer, Frontend Engineer, Full Stack Engineer, Data Scientist, Cloud Engineer, Cyber Security Analyst), validated against the real, current database (not memory or the Phase 24 blueprint alone — directly queried: only 3 `LearningPath` rows exist platform-wide, none of these 6; 35 non-production E2E/test-fixture course rows found and disclosed as noise, excluded from analysis). **Course counts needed are not uniform** — validated per path from a real skill-gap table: Frontend Engineer needs only 1 new course (Programming Foundations, reusing 2 already-real courses), Data Scientist needs 3 new courses (the least existing-content reuse of the 6). **7 distinct new courses identified across all 6 paths**, each mapped to specific missing skills with zero duplicate assignments. **Two real architecture gaps found and documented, not fixed** (per this phase's explicit scope): `Certificate` has no `learningPathId` field (no path-level certificate mechanism exists in the real schema), and `Project` has no `learningPathId` field (no path-level capstone mechanism exists) — both confirmed by directly reading `schema.prisma`. Recommended priority order: Frontend Engineer → Backend Engineer → Full Stack Engineer → Cloud Engineer → Cyber Security Analyst → Data Scientist. Full report: `docs/content-library/phase35-learning-path-master-blueprint.md`. See `docs/restore-point-phase35.md`. **Explicitly stopped, awaiting approval and direction before Phase 36.** Previous phase:

**Phase 34 — Educational Content Production: Computer Networking Foundations — complete. Brought Computer Networking Foundations to production-ready status (3 of 3 planned modules)** — the 6th course this session declares fully complete end-to-end (after Prompt Engineering, Phase 27; UI/UX Design Foundations, Phase 30; DevOps Foundations, Phase 31; Full-Stack Web Development with Next.js, Phase 32; AI Foundations, Phase 33), and this closes out **all 6 of the original Phase 25 courses to full production-ready status.** Added Module 2 (Routing, Switching & DNS) and Module 3 (HTTP/HTTPS, VPNs & Troubleshooting) — 8 new lessons, 2 new quizzes/10 questions, 2 new standalone Projects (the course's first, bringing it to its blueprint total of 2). Two new resources (IETF RFC 1035 for DNS, RFC 9110 for HTTP Semantics) were live-verified via `WebFetch` before citing; a third candidate (Cloudflare Learning Center) could not be verified (HTTP 403, bot-blocked) and was explicitly excluded rather than cited unverified. **Validated for real:** idempotent seed (0 duplicates on rerun; Phase 25/27/30/31/32/33 seeds all re-confirmed idempotent), direct database duplicate-title/duplicate-prompt checks found zero duplicates at every level, 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey — enrolled for real, all 13 lessons completed through the real lesson/quiz flow (one quiz first failed on purpose, confirmed no false-positive completion), **completion reached 100%**, a **brand-new certificate issued from zero**, and certificate-issuance idempotency directly re-confirmed. A real project was submitted, self-evaluation was confirmed blocked (403), and the real owning instructor evaluated it for real with persistence confirmed. Full report: `docs/phase34-networking-content-production-report.md`. See `docs/restore-point-phase34.md`. **Explicitly stopped, awaiting approval before Phase 35.** Previous phase:

**Phase 33 — Educational Content Production: AI Foundations — complete. Brought AI Foundations to production-ready status (4 of 4 planned modules)** — the 5th course this session declares fully complete end-to-end (after Prompt Engineering, Phase 27; UI/UX Design Foundations, Phase 30; DevOps Foundations, Phase 31; Full-Stack Web Development with Next.js, Phase 32). Added Modules 2-4 (Search & Planning Algorithms; Knowledge Representation & Intelligent Agents; AI Ethics, Safety & Applied AI Survey) — 12 new lessons, 3 new quizzes/15 questions, 3 new standalone Projects (the course's first, bringing it to its blueprint total of 3). Two new resources (the official AIMA textbook website, NIST's AI Risk Management Framework) were live-verified via `WebFetch` before citing. **Validated for real:** idempotent seed (0 duplicates on rerun; Phase 25/27/30/31/32 seeds all re-confirmed idempotent), direct database duplicate-title/duplicate-prompt checks found zero duplicates at every level, 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey — enrolled for real, all 17 lessons completed through the real lesson/quiz flow (one quiz first failed on purpose, confirmed no false-positive completion), **completion reached 100%**, a **brand-new certificate issued from zero**, and certificate-issuance idempotency directly re-confirmed. A real project was submitted, self-evaluation was confirmed blocked (403), and the real owning instructor evaluated it for real with persistence confirmed. Full report: `docs/phase33-ai-foundations-content-production-report.md`. See `docs/restore-point-phase33.md`. **Explicitly stopped, awaiting approval before Phase 34.** Previous phase:

**Phase 32 — Educational Content Production: Full-Stack Web Development with Next.js — complete. Brought Full-Stack Web Development with Next.js to production-ready status (6 of 6 planned modules)** — the 4th course this session declares fully complete end-to-end (after Prompt Engineering, Phase 27; UI/UX Design Foundations, Phase 30; DevOps Foundations, Phase 31), and previously the platform's single most partially-authored course (1 of 6). Added Modules 2-6 (JavaScript & TypeScript Fundamentals; Frontend Frameworks/React; Backend Web Frameworks & REST API Design; Full-Stack Frameworks/Next.js; Web Performance & Security Basics) — 21 new lessons, 5 new quizzes/25 questions, 5 new standalone Projects (bringing the course to its blueprint total of 6). Four new documentation resources (React, TypeScript, Node.js API docs, OWASP Top Ten) were live-verified via `WebFetch` before citing. **Validated for real:** idempotent seed (0 duplicates on rerun; Phase 25/27/30/31 seeds all re-confirmed idempotent against the now-6-module course), a direct duplicate-title check found zero duplicates at every level, 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey — enrolled for real, all 25 lessons completed through the real lesson/quiz flow (one quiz first failed on purpose, confirmed no false-positive completion), **completion reached 100%**, a **brand-new certificate issued from zero**, and certificate-issuance idempotency directly re-confirmed. A real project was submitted, self-evaluation was confirmed blocked (403), and the real owning instructor evaluated it for real with persistence confirmed. Full report: `docs/phase32-fullstack-content-production-report.md`. See `docs/restore-point-phase32.md`. **Explicitly stopped, awaiting approval before Phase 33.** Previous phase:

**Phase 31 — Educational Content Production: DevOps Foundations — complete. Brought DevOps Foundations to production-ready status (5 of 5 planned modules)** — the 3rd course this session declares fully complete end-to-end (after Prompt Engineering, Phase 27; UI/UX Design Foundations, Phase 30). Added Module 3 (Container Orchestration/Kubernetes), Module 4 (Infrastructure as Code & Configuration Management), and Module 5 (Monitoring, Observability & SRE Basics) — 13 new lessons, 3 new quizzes/15 questions, and 3 new standalone Projects (bringing the course to its blueprint total of 5). Three new documentation resources (HashiCorp Terraform docs, Prometheus docs, Google's free official SRE Book) were live-verified via `WebFetch` before citing. **Validated for real:** idempotent seed (0 duplicates on rerun; Phase 27/30 seeds re-confirmed idempotent against the now-5-module course), 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey — enrolled for real, all 24 lessons completed through the real lesson/quiz flow (one quiz first failed on purpose, confirmed no false-positive completion), **completion reached 100%**, a **brand-new certificate was issued from zero**, and repeated-completion idempotency was directly re-confirmed (re-marking an already-complete lesson created no duplicate certificate). A real project was submitted, self-evaluation was confirmed blocked (403), and the real owning instructor evaluated it for real. Full report: `docs/phase31-devops-content-production-report.md`. See `docs/restore-point-phase31.md`. **Explicitly stopped, awaiting approval before Phase 32.** Previous phase:

**Phase 30 — Educational Content Production: UI/UX Design Foundations — complete. Brought UI/UX Design Foundations to production-ready status (4 of 4 planned modules)** — the 2nd course this session declares fully complete end-to-end (after Prompt Engineering, Phase 27). Added Module 3 (Prototyping, Interaction Design & Accessibility — closes the accessibility gap Modules 1/2 explicitly deferred there) and Module 4 (Design Systems & Usability Testing), 9 new lessons, 2 new quizzes/10 questions, and 2 new standalone Projects (bringing the course to its blueprint total of 3). Two new documentation resources (W3C WCAG 2.2 Quick Reference, Nielsen Norman Group's usability heuristics) were live-verified via `WebFetch` before citing — no fabricated resources. **Validated for real:** idempotent seed (0 duplicates on rerun; Phase 27's seed re-confirmed idempotent against the now-4-module course too), record counts verified before/after by direct query, 241/241 backend tests unchanged, both builds clean, and a full live learner+instructor journey — enrolled for real, all 19 lessons completed through the real lesson/quiz flow (one quiz first failed on purpose to confirm no false-positive completion), **completion reached 100%**, and — closing the one residual gap Phase 29's report flagged — **a brand-new certificate was issued from zero** (not just an idempotent re-confirmation of a pre-existing one). A real project was submitted, self-evaluation was confirmed blocked (403), and the real owning instructor evaluated it for real. Full report: `docs/phase30-uiux-content-production-report.md`. See `docs/restore-point-phase30.md`. **Explicitly stopped, awaiting approval before Phase 31.** Previous phase:

**Phase 29 — Fix Quiz-Lesson Completion & Certificate Eligibility — complete. Closed the one limitation Phase 28 deliberately documented rather than bypassed.** Root cause: `submitQuizAttempt` scored a quiz and recorded a `QuizAttempt` but never touched `LessonProgress`, so no course with a quiz-type lesson could ever reach 100% completion or issue a new certificate. Fix: extracted the shared "upsert lesson progress → recompute completion → notify + attempt certificate issuance" flow that `updateLessonProgress` already used into one private method, and call it from `submitQuizAttempt` whenever an attempt passes. No parallel completion mechanism, no certificate-rule change, no schema change — `certificatesService.issueForEnrollment`'s existing independent quiz-passing re-validation was already correct and untouched. **Validated for real, on the same real course Phase 28 found capped at 92%:** confirmed still 92% (23/25) at the start; a deliberately wrong quiz submission left it at 92% (no false-positive completion); the real correct answers for its two remaining quiz lessons raised completion to 96% then **100% (25/25)**, entirely through the real quiz-submission flow — and the resulting 100%-completion transition correctly triggered the existing certificate-issuance path (idempotently returned the enrollment's existing certificate, no duplicate). Backend tests: 241/241 (237 prior + 4 new regression tests, zero regressions). Backend tsc/lint/build clean. Frontend tsc/lint/build all clean (no frontend code changed this phase — this also retroactively confirms the one Phase 28 CSS edit that couldn't be build-verified last session is fine). Full report: `docs/phase29-completion-fix-report.md`. See `docs/restore-point-phase29.md`. **Explicitly stopped, awaiting approval before Phase 30.** Previous phase:

**Phase 28 — Educational Frontend Experience — complete. First complete learner-facing UI over the Phase 26/27 educational backend — Learning Paths → Course → Lesson → Quiz → Project → Instructor Evaluation, built almost entirely by reusing existing routes/components/design system.** Two approved backend changes, both found necessary/broken via genuine live verification, not planned in advance: (1) `GET /progress/quizzes/:quizId` (new — no quiz-read endpoint existed) + a `quizId` field added to `GET /lessons/:id`; (2) a real, pre-existing scoring bug fixed in `quiz-scoring.ts` — every `single`-type question's seeded `correctAnswer` was stored as a one-element array but scored via strict equality against a bare-string submission, so **no learner could ever pass a single-choice quiz question** before this fix. Both approved via `AskUserQuestion` before implementation. **A second real gap was found and deliberately left undone, per this phase's own "document, don't bypass" instruction for certificates:** passing a quiz never marks its lesson complete anywhere in the codebase, so any course containing a quiz-type lesson cannot reach 100% completion or trigger new certificate issuance — confirmed live (completion capped at 92%, 23/25 lessons, after marking every non-quiz lesson complete). **Validated for real:** backend tests 237/237 (234 prior + 3 new regression tests, the original 230 from Phase 27 all still pass), backend tsc/lint/build clean, frontend tsc/lint clean, and a full live learner+instructor journey via direct HTTP calls against the real running API (no browser-automation tool available this session) — login → real learning paths → real course/lesson (confirmed new `quizId` field) → real quiz via the new endpoint (confirmed no `correctAnswer` leaked) → real submitted attempt (caught the scoring bug) → real corrected score after the fix → real `maxAttempts` enforcement → real project submission → learner genuinely blocked (403) from self-evaluation → real instructor evaluation → confirmed persisted. Full report: `docs/phase28-educational-frontend-report.md`. See `docs/restore-point-phase28.md`. **Explicitly stopped, awaiting approval before Phase 29.** Previous phase:

**Phase 27 — Educational Content Production & Platform Population — complete. Real content production against the Phase 26 architecture — no schema changes, zero regressions.** Brought **Prompt Engineering: Mastering Large Language Models to full completion** (4 of 4 modules, 25 lessons, 4 quizzes, 20 questions, 4 projects) — the platform's first course declared production-ready end-to-end. Added Module 2 to UI/UX Design Foundations and to DevOps Foundations (both explicitly still partial, not overclaimed). Created the platform's **first 2 standalone `Project` rows** (RAG Pipeline with Citations; Production Prompt-Powered Feature Capstone) directly via the real Phase 26 architecture — no lesson-workaround. **Totals added:** 5 modules, 25 lessons, 5 quizzes, 25 questions, 2 projects. **Validated for real:** idempotent seed (0 duplicates on rerun, Phase 25/26 seeds re-confirmed idempotent too), record counts verified before/after by direct query, 230/230 backend tests unchanged, both builds clean, and a full live learner journey (login → path → 4-module course → real new lesson → real quiz at 100% → view + submit a real standalone project → RBAC-verified instructor evaluation, learner self-grading correctly blocked with 403). Full report: `docs/phase27-educational-content-production-report.md`; per-course completion status: `docs/content-library/phase27-content-inventory.md`. See `docs/restore-point-phase27.md`. 12 of 18 blueprint categories and 6 of 9 named learning paths remain untouched, deliberately, per this phase's own scope-control instruction. **Explicitly stopped, awaiting approval before Phase 28.** Previous phase:

**Phase 26 — Learning Path & Project Evaluation Architecture — complete. Real schema + backend implementation, closing exactly the two architecture gaps Phase 25 documented — nothing else touched.** 5 new Prisma models (`LearningPath`, `LearningPathCourse`, `Project`, `ProjectSubmission`, `ProjectEvaluation`), one clean additive migration, two new backend modules (`learning-paths`, `projects`), `docs/16-API-CONTRACT.md` §20–21 written before implementation (matching this codebase's own convention), 2 new permission keys (content_editor/admin only), 21 new unit tests. **The 6 existing Phase 25 project briefs were never duplicated** — each is linked via a nullable, unique `sourceLessonId` FK; `Project.instructions` is `null` for all 6, verified directly. **Validated for real:** 230/230 backend tests (209 existing + 21 new, zero regressions), clean tsc/lint/backend-build/frontend-build, migration applied with pre/post record counts identical (41/31/47/6/30/1/1), both new seed scripts run twice with zero duplicates on the second run, and a live API smoke test of the full submit→evaluate→409-on-redo flow against the real running backend. Full report: `docs/phase26-learning-path-project-architecture-report.md`. See `docs/restore-point-phase26.md`. **No ambiguity required stopping this phase** — every design decision was resolved from an existing codebase convention, cited inline. **Explicitly stopped, awaiting approval before Phase 27.** Previous phase:

**Phase 25 — Educational Content Verification & First Production Content — complete. Real content authoring + a controlled, additive database seed (not documentation-only).** `apps/api/src` untouched, 209/209 backend tests still pass; one new file (`apps/api/prisma/seed-phase25-content.ts`). Seeded the platform's **first real, non-test-fixture educational content**: 6 courses (Prompt Engineering, AI Foundations, UI/UX, Full-Stack Web Dev, Networking, DevOps), 37 real lessons, 6 quizzes/30 questions, 6 project briefs — spanning 3 learning paths. **Headline finding:** real, documented (not invented) schema gaps — no `LearningPath` model, no `Project`/submission-grading model, `Course` has no difficulty/hours/tags fields. Per this phase's instruction, these were stopped-and-documented, not worked around with a schema change. **Two stale URLs corrected:** OpenAI's and Anthropic's official docs have both moved since Phase 24 — found via live `WebFetch` verification, `documentation-links.md` updated. **Validated for real:** seed script run twice, zero duplicates on the second run; a complete live learner journey (login → find course → enroll → read 8 real lessons → pass a real quiz at 100% → reach 100% completion → receive a real, auto-issued certificate) executed against the live API, not simulated. Full report: `docs/phase25-content-production-report.md`. See `docs/restore-point-phase25.md`. **Explicitly stopped, awaiting approval before Phase 26.** Previous phase:

**Phase 24 — Complete Educational Content Library — complete. Documentation/planning only — zero application code, database schema, or seed data touched.** A new, independent thread (does not block or depend on the deployment-readiness track below). Built the master content blueprint for Phoenix's educational content in `docs/content-library/` (11 files): 18 categories, 9 professional learning paths (AI Engineer, Backend Engineer, Frontend Engineer, Full Stack Engineer, Data Scientist, Cloud Engineer, Cyber Security Analyst, DevOps Engineer, Prompt Engineer), 18 fully-specified flagship courses, a proven lesson-authoring template (2 courses' opening modules fully authored as worked examples, ~569 remaining lessons honestly scoped as future work), 36 tiered practical projects, a confidence-flagged books/videos library (🟢/🟡 flags, zero fabricated ISBNs or video URLs — one category left honestly incomplete rather than guessed), 20 official documentation entry points, a two-tier certificate policy (75% passing standard), an 8-phase content-production roadmap (~12–15 months total), and a normative quality-standards rubric. **Validation:** confirmed zero duplicate course titles, zero duplicate book titles, consistent difficulty/prerequisite chains, via direct scan. Full report: `docs/phase24-content-library-report.md`. See `docs/restore-point-phase24.md`. **Explicitly stopped, awaiting approval before Phase 25.** Previous phase:

**Phase 23.1 — Deployment Package Finalization — complete. Verdict: 🟢 PASS (zero broken links).** Fixed all 59 broken references Phase 23's dry run found, plus 6 more caught by a follow-up automated validation script (including 2 real authoring bugs in Phase 22's own new files — missing `../` prefixes in `required-service-accounts-summary.md` and `final-checklist-before-launch.md`). **Final result: 0 broken references out of 141 checked** (60 resolve in-package, 27 explicitly labeled as main-repository references, 54 historical restore-point narrative covered by one new `05-Restore-Points/README.md` disclaimer rather than invasive historical-text edits). Resolved the one real structural ambiguity Phase 23 flagged — added an explicit "this package requires the full Phoenix repository alongside it" statement to `README.md`, `handover-guide.md`, and `production-readme.md`. Documentation-only; zero application code touched. Full report: `docs/phase23.1-deployment-package-finalization-report.md`. See `docs/restore-point-phase23.1.md`. **The deployment package is now complete and can be executed by an external operator without additional project knowledge.** **Explicitly stopped, awaiting approval before Phase 24.** Previous phase:

**Phase 23 — Deployment Dry Run (Read-Only Validation) — complete. Verdict: 🟡 PASS WITH WARNINGS.** Performed a full deployment rehearsal as a brand-new operator using `Phoenix-Deployment-Package/` as the only reference. **Headline finding:** the package's content is complete and accurate (every technical claim verified against the real system, including actually executing `scripts/verify-deployment-readiness.js` for the first time — it works correctly), but its internal cross-references are not: **59 broken `docs/`/`scripts/` path references across 9 of 10 operational documents** (all copied verbatim from `docs/` in Phase 22; the 3 documents authored fresh for the package have zero broken references), plus one structural ambiguity — the bundled smoke-test scripts assume a full repository checkout, and the package never says so explicitly. Deployment order and rollback plan both validated as complete and internally consistent. Full report with a 6-item numbered fix list: `docs/phase23-deployment-dry-run-report.md`. See `docs/restore-point-phase23.md`. **No documents were rewritten this phase** (per its own constraint) — fixes are deferred to Phase 24, pending approval. **Explicitly stopped, awaiting approval before Phase 24.** Previous phase:

**Phase 22 — Final Deployment Package & Operator Bundle — complete. Packaging only — zero new code, zero new engineering decisions.** Consolidated every Phase 20/21 deliverable into a single, self-contained, professionally organized folder at the repo root: **`Phoenix-Deployment-Package/`** (README + 9 numbered subfolders: Launch Guide, Production Checklist, Handover Guide, Smoke Test, Restore Points, Environment Variables, Required Service Accounts, Deployment Order & Rollback, Final Launch Checklist). Written so a technical operator with zero prior project knowledge (the user's partner in Turkey) can execute deployment without asking a question — README and two new synthesis pages (service-accounts summary, final printable launch gate) are bilingual (Arabic/English); everything else is a direct, unmodified copy of already-validated Phase 20/21 work. See `docs/restore-point-phase22.md`. **Per the user's own stated sequence: real production deployment does not begin until this package is explicitly approved.** Previous phase:

**Phase 21 — Production Smoke Test & Launch Automation — complete. Implementation + documentation — real, runnable tooling (verified via `git status`: no business logic/API/schema/UI changed, purely additive).** ⚠️ **Naming collision (fourth "Phase 21" this session)** — sibling to the three deliverables below, not a replacement; see `docs/restore-point-phase21-smoke-test.md`. Built `scripts/production-smoke-test.js` (PASS/FAIL/WARN across Frontend/Backend/Database/Storage/Notifications/Certificates/Payments/AI/Security/Authentication, read-only by default, `--deep` mode for fixture-authenticated checks, never fabricates results) and `scripts/verify-deployment-readiness.js` (orchestrates build → env-check → DB validation → health → smoke test → summary, reusing existing Turbo/Jest/Prisma tooling). Also created `docs/production-checklist.md` (provider-specific: Vercel/Railway/Postmark/Neon/Backblaze), `docs/handover-guide.md` (zero-project-knowledge operator guide), and `docs/production-verification-report-template.md`. **Validation, real and executed:** backend build clean, **209/209 backend tests passing**, frontend build clean, smoke test run against a live local environment — **17 PASS, 7 WARN, 0 FAIL, overall PASS WITH WARNINGS.** One real finding investigated (not just reported): an apparent homepage 500 traced to a stale `next dev` artifact, confirmed non-issue against a real `next build`+`next start` production-mode server (returns the correct 307). **Explicitly stopped, awaiting approval before any deployment phase.** Previous phase:

**Phase 21 — Production Infrastructure Preparation — complete. Documentation + placeholder infrastructure only, zero application code changed (verified via `git status`).** ⚠️ **Naming collision, flagged (third "Phase 21" this session)** — see `docs/restore-point-phase21-infra-prep.md`'s note; this is a sibling to, not a replacement of, the two Phase 21 deliverables below. Created production environment templates (`.env.production.example`, `.env.server.production.example`, root, placeholder values only), `docs/production-secrets-checklist.md` (every secret's purpose/generation/storage/rotation), `docs/deployment-checklist.md` (full pre/infra/database/storage/email/DNS/SSL/monitoring/backups/post-deployment/rollback checklist), a `deployment/` placeholder folder (`vercel/`, `railway/`, `backups/`, `dns/`, `ssl/`, `monitoring/`, no real secrets), `docs/production-readme.md` (zero-assumption step-by-step deployment guide + health-endpoint docs), and `docs/launch-package.md` (one-page summary of accounts/credentials/domains/services/order/duration). Full report and validation: `docs/restore-point-phase21-infra-prep.md`. **Explicitly stopped, awaiting approval before Phase 22.** Previous phase:

**Phase 21 — Launch Preparation (Pre-Deployment) — complete. Read-only, zero code changes.** ⚠️ **Naming collision, flagged not resolved:** this reuses the "Phase 21" number already used by the v1.1 strategic-planning report below — the two are siblings (forward strategy vs. this document's pre-deployment operational checklist), filed under distinct restore points (`restore-point-phase21-launch-prep.md` vs. `restore-point-phase21.md`). Produced a full repository-health cleanup list (nothing deleted — `Phoenix_v0.1.0.zip`, `.preview/`, `.vercel/` newly flagged alongside the already-known `shots/`/`shots2/`), a 15-service environment checklist, a production-secrets table (zero real values), a 12-step deployment order, a role-by-role smoke-test plan, monitoring and disaster-recovery plans, a launch-day checklist, a 5-tier post-launch monitoring checklist, and a Go/No-Go matrix. **Launch Readiness: ~72%** — engineering is ready (209/209 tests, zero Critical findings across two audits); the gap is entirely external (hosting, domain, DNS, production database/bucket, real Postmark/Stripe credentials, monitoring). **Estimated time to production: 1–2 business days of owner-side setup.** Full report: `docs/phase21-launch-preparation.md`. See `docs/restore-point-phase21-launch-prep.md`. **Explicitly stopped, awaiting approval before Phase 22.** Previous phase:

**Phase 21 (v1.1 Strategic Planning) — complete. Read-only, zero code changes.** A deep, non-repeating planning pass for what comes after the v1.0 freeze: 18 named architecture areas each with Current/Future State + Why/Risk/Priority/Effort, a feature roadmap framed by business/technical value (Critical/Important/Nice-to-Have/Future Research), new technical debt items not previously documented (a repeated locale-resolution pattern across `apps/web` pages, hardcoded rate-limit values, no feature-flag mechanism), security/performance/AI/UX-specific roadmaps (recommendations only, nothing implemented), scalability reasoning at 5 user-count tiers (100 → 1M, architectural reasoning since no load testing has ever been run), a testing roadmap, a release/versioning strategy, and an executive recommendation. **Headline conclusion: the three highest-leverage v1.1 items are the Workers/Notifications-delivery foundation, production monitoring, and the Next.js Critical dependency upgrade — all buildable without further product decisions, unlike Search or AI expansion.** Full report: `docs/phase21-v1.1-roadmap.md`. See `docs/restore-point-phase21.md`. Does not change Phase 19's launch status (still 🟡 LAUNCH POSTPONED, owner-blocked) — v1.1 planning and launch execution remain two independent threads. Previous phase:

**Phase 20 (Final Architecture Lock & Version 1.0 Freeze) — complete. Phoenix Platform Version 1.0 is officially frozen.** Documentation, verification, and freeze only — zero application code changes. Produced a real, counted project inventory (151 frontend files, 203 backend files across 23 modules, 52 database models, 21 migrations), a full feature inventory by module with status/evidence, an architecture freeze description, a technical debt table (12 items), known limitations, a documentation audit, code-health scores across 8 dimensions, production-readiness scores across 7 dimensions (**Overall: 83/100**), and a v1.1+ roadmap. **Executive conclusion, in full: v1.0 is officially frozen; future development can safely build on it; the architecture is stable (zero Critical findings across two independent live security audits); the codebase is worth continuing to build on.** Full documents: `docs/version-1.0-freeze.md`, `docs/version-1.0-inventory.md`, `docs/version-1.0-roadmap.md`. See `docs/restore-point-phase20.md`. Previous phase:

**Phase 19 (Production Deployment Execution) — complete, as an Execution Readiness simulation. Final Decision: 🟡 LAUNCH POSTPONED.** This phase attempted the real production deployment Phase 18's conditions pointed toward, and — exactly as its own addendum anticipated — hit a hard, correct stop at every step requiring an external account: no hosting provider account (Vercel/Railway/etc.), no domain, no DNS access, no Postmark account, no separate production database/storage bucket. Per the addendum, no E2E fixtures were used to simulate a production smoke test and no fake production data or secrets were generated. **What was actually done, real and verified:** deleted the confirmed-stale `apps/web/vercel.json` (only after explicit user confirmation of the exact file, since the deletion was blocked by the permission classifier as destructive) — modern Next.js needs no such file; final clean rebuild of both apps (209/209 backend tests, clean lint/type-check, all 60 frontend routes) confirms zero regression from the removal. **The full deliverable is a precise, 16-item, priority-ordered Owner Action List** (`docs/phase19-execution-readiness-report.md`) — every remaining step is an external account, a credential, or a decision that belongs to the project owner, not engineering work. **Phoenix v1.0's core development remains complete**, unaffected by this phase's postponement — this is a launch-execution blocker, not a development one. Previous phase:

**Phase 18 (Release Verification & Dry Run) — complete. Final Decision: 🟡 GO WITH CONDITIONS.** A live, end-to-end release rehearsal — not development — across 8 domains: all 4 user-role journeys (Learner/Instructor/Moderator/Admin) verified live via real browser automation, the full API surface swept live (11 categories, all 200), security re-verified live (9/9 checks, zero regression), the database re-verified live against the real Neon instance (5/5 checks), documentation cross-checked against real code (5/5 accurate), and the Phase 17 deployment guide walked through by a simulated newcomer. **Exactly one genuine launch-blocking finding, fixed:** `.gitignore` didn't cover the `.env.production` filename pattern — a real secrets-leak risk the Phase 17 guide had incorrectly called "safe by construction." Fixed with one additive line, verified before/after with `git check-ignore`. **One real, confirmed, non-blocking defect found:** the header's dark-mode toggle is decorative only — directly measured (computed styles + pixel comparison) to have zero functional effect, a suspicion carried since Phase 14.6B, now confirmed. Two apparent E2E failures in the first test pass were investigated and found to be test-harness timing bugs, not product bugs (both directly reproduced as working correctly). 209/209 backend tests unaffected. **Phoenix v1.0's core development is confirmed complete** — remaining conditions are operational (owner decisions from Phase 17, a stale `vercel.json`, an undocumented monorepo-config step, the dark-mode fix), not unfinished functionality. Full report: `docs/phase18-release-verification-report.md`. Previous phase:

**Phase 17 (Deployment & Production Launch Planning) — complete.** Pure planning/documentation — zero code changes, per the phase's own "operational, not feature development" scope and its explicit instruction not to create hosting-provider-specific files or assume decisions that belong to the project owner. Produced a full hosting comparison (Vercel/Railway/Render/Fly.io/Azure/AWS), recommending **Vercel (frontend) + Railway (backend/workers)** — a recommendation for the owner to confirm, not a decision made. Also produced the full environment/database/storage/email/monitoring/security guides, a Production Checklist, a 3-stage Launch Checklist, Rollback/Backup/Recovery plans, and a **Go/No-Go Decision: No-Go today**, blocked entirely on 4 owner decisions (hosting provider, domain, DNS access, fresh production secrets) — none requiring further engineering work. Real findings surfaced while writing the guide: no Dockerfile exists anywhere despite the architecture doc committing to containerizing the backend; `apps/web/vercel.json` is a stale, actively-wrong leftover from the pre-Phoenix scaffold project; Terraform remains scaffolding-only (manual infra provisioning, not reproducible from code); certificates have no real PDF generation (`pdfFileId` always `null`). Full guide: `docs/phase17-deployment-launch-guide.md`. Previous phase:

**Phase 16 (Release Candidate 1 — Production Launch Readiness) — complete.** Closed all 5 Critical blockers from `docs/phase15-production-readiness-report.md`: real Postmark email integration (wired into verification/password-reset/MFA notifications, safe-degrading when unconfigured), real HTTP security headers live-verified on both apps (helmet + CSP/HSTS/etc.), a real `npm audit` (27 findings, all requiring breaking major upgrades — none forced mid-RC, all documented with reasoning), CI/CD reviewed and improved (Turbo caching + build artifacts added; no deployment workflow exists, correctly identified as needing a hosting-target decision rather than invented), and 3 new database indexes applied to the live database and independently verified. Zero regressions: 209/209 backend tests (up from 201), clean builds/lint/type-check on both apps, and a live multi-role Playwright pass with zero CSP violations. **Engineering recommendation: B — Ready after minor operational tasks** (real Postmark credentials, a hosting-target decision, the deferred dependency upgrades, CI verified on a real push — none requiring new code design). Full report: `docs/phase16-production-validation-report.md`. Previous phase:

**Phase 15 (Platform Stabilization & Production Readiness) — complete.** A fourth, standalone thread — a full production-readiness audit (repository health, dependencies, performance, security, API, database, documentation, testing, risk assessment), **read-only, zero code changes**, per this phase's explicit instruction. Full findings in `docs/phase15-production-readiness-report.md`. Headline result: **Deployment Readiness 58/100, Production Readiness 74/100 — not ready to launch today**, blocked by 5 Critical, bounded items (no email provider configured, no HTTP security headers, `npm audit` never run, CI never verified on a real remote push, two missing database indexes), none of which require architectural rework. Two errors in the raw audit findings were caught and corrected before publishing (a false "empty migrations directory" claim, and an overbroad "entire legacy route tree is dead" claim that missed two real Stripe redirect routes) — see the report for the verification method. Previous phase:

**Phase 14.8 (Final Platform Polish Completion) — substantial progress, not fully complete (superseded as "current" by 15 above, history preserved).** Continues the Phase 14.7 thread: all 7 form pages (Login/Register/Checkout/Profile/Settings/Forgot/Reset Password) now use a real glass-card treatment built from Phase 14.7's tokens; every remaining ad-hoc empty state (13 pages) was replaced with the shared `EmptyState` component; new `Spinner`/`Skeleton` components were built and applied to 7 list/detail pages; Orders/Admin Users/Audit Logs were investigated and found to already have real search/filter/pagination (a prior audit's "missing" claim was corrected, not fixed, since it was inaccurate). Dashboard unification (Moderator/Admin), a full responsive review, and a full accessibility audit remain open — see `docs/restore-point-phase14.8.md`'s "Remaining Visual Issues" for all 8 explicitly disclosed items. Previous phase:

**Phase 14.7 (Platform Final Polish & UI Completion) — partial, in progress (superseded as "current" by 14.8 above, history preserved).** This is a *third*, separate informal numbering track (see the naming note below) — a visual/UX audit-and-polish thread (14.6 Visual Review → 14.6B Pixel Audit → 14.6C Review Gallery → 14.7 Implementation) that ran independently of the `docs/phase-14-plan.md` Candidate track (A/B/F/C done, **D — Notifications delivery — is still the next item on that track, unaffected by this one**). Phase 14.7 implemented the Critical/High findings from the visual audits: the header auth-state bug, the tablet header-overflow bug, a shared StatusBadge component (applied to 3 screens), a shared EmptyState component (applied to 2 screens), a first centralized design-token pass, and a test-data visibility fix on Admin Users. **Not a full implementation of every item in the Phase 14.7 brief** — see `docs/restore-point-phase14.7.md` for exactly what was and wasn't done, and why the rest was deliberately deferred rather than rushed. Candidates A (Documentation Reconciliation), B (Admin MFA), C (`apps/admin` resolution), and F (CI) remain closed and unaffected. **The "Completed" log below this section is preserved unchanged as the historical phase-by-phase record — it is not rewritten, only supplemented.**

**Naming note (unresolved, flagged not fixed — see `docs/phase-14-plan.md` §1.0):** this project has (now) three independent, conflicting phase-numbering systems — the formally approved 14-phase roadmap in `docs/17-IMPLEMENTATION-ROADMAP.md` (where Phase 14 means "Production Deployment"), this document's own informal numbering for the Candidate track (where "Phase 14.1"–"14.4" cover Documentation Reconciliation/MFA/CI/`apps/admin`), and a third, separate informal thread for the visual-review/polish work (Phase 14.6/14.6B/14.6C/14.7, this entry). Not resolved by this phase; flagged again here so the next session doesn't inherit it silently or assume "Phase 14.7" continues the Candidate D/E queue.

---

## Platform Implementation Status (added Phase 14.1 — whole-platform view)

Verified directly against the repository this session: `git log`/`git status`, `apps/api/src/modules/*` directory listing, `npx prisma validate` (51 models, schema valid), a fresh `npm test` run in `apps/api` (**22/22 suites, 166/166 tests passing**), and direct inspection of `apps/admin`, `apps/workers`, and `.github`.

| Subsystem | Status | Evidence |
|---|---|---|
| **Auth** (register/login/refresh/logout/password reset, RS256 JWT, session management, RBAC) | Implemented, tested | `modules/auth`, `modules/sessions`, `modules/refresh-tokens`, `modules/roles`, `modules/permissions` — unit-tested; live-verified for the flows Phase 11 exercised (Course Editor, Moderator) |
| **MFA** | **Implemented (Phase 14.2), tested, live-verified** | Two-step TOTP challenge/response (docs/10-SECURITY-BIBLE.md §5), the architecture selected in Phase 14.1's Security Architecture Review. Real enrollment/confirm/disable/recovery-code endpoints; TOTP secret encrypted at rest (AES-256-GCM, `MFA_ENCRYPTION_KEY`); 10 single-use, hashed recovery codes; every state change audit-logged. 35 new backend tests (201/201 total passing) + a real, live 15-check end-to-end verification against the running API with real generated TOTP codes (not mocked). **Enrollment remains opt-in for every role, including admin-capable ones** — see "Scope decision" in `docs/known-issues.md`'s MFA entry for why this doesn't yet satisfy §5's "mandatory" wording literally. |
| **Courses / Lessons / Modules / Quizzes** | Implemented, tested | `modules/courses` (includes lesson/module/quiz management) — unit-tested; live-verified for Course Editor + video-lesson creation (Phase 13.3/13.4) |
| **Enrollments / Progress / Certificates** | Implemented, tested | `modules/enrollments`, `modules/progress`, `modules/certificates` — unit-tested, not independently live-verified beyond what unit tests cover |
| **Library** | Implemented, tested | `modules/library` — unit-tested |
| **Marketplace** (Products/Orders/Payments incl. Stripe refunds) | Implemented, tested | `modules/products`, `modules/orders`, `modules/payments` — real `POST /admin/payments/:id/refund` endpoint, permission-gated (`order:refund`), pre-checks against Stripe before calling it; unit-tested |
| **Media / Object Storage** | **Complete, fully live-verified** | Phases 13.1–13.8; real upload→storage→complete chain verified against Backblaze B2 with zero mocks (Phase 13.7) |
| **News** | Implemented, tested | `modules/news` — unit-tested |
| **Notifications** | **Partial — API only** | `Notification` model exists (`channel: in_app\|email\|push`) with create/list endpoints; **no delivery worker, no email provider configured, no preferences table** |
| **AI Gateway** | Implemented, tested | `modules/ai` — unit-tested |
| **Admin (`apps/web` route tree)** | **Implemented, tested, permission-gated — the platform's one authoritative admin surface (Phase 14.4)** | `apps/web/src/app/[lang]/admin/*` — analytics, audit-logs, settings, users; backed by `modules/admin` (unit-tested); re-verified building and passing all checks unchanged after Phase 14.4's decision |
| **Admin (`apps/admin`, standalone app)** | **Retired (Phase 14.4)** | Formally superseded — `docs/09-PLATFORM-ARCHITECTURE.md` §16 updated to reflect it. Directory kept in place (not deleted) as inert history; removed from CI's build gate. See `docs/restore-point-phase14.4.md` for the full architecture review and reasoning. |
| **Background workers** (`apps/workers`) | **Empty shell** | `src/main.ts` only; its own `dev` script prints "apps/workers has no job processors yet" |
| **Search** | **Not implemented** | Meilisearch is provisioned in `infra/docker/docker-compose.yml` only — zero backend module, zero indexing, zero query endpoint (confirmed by grep, not assumed) |
| **CI / automated quality gate** | **Implemented (Phase 14.3), live-verified** | `.github/workflows/ci.yml` — Prisma validate, type-check, lint, format-check, backend unit tests, backend + frontend builds. ESLint/Prettier newly wired (previously executed 0 tasks); every command verified locally with the exact env vars CI uses, no real `.env`. All 5 failure categories (lint/type/test/schema/build) confirmed to genuinely fail when broken, then confirmed clean when restored. |
| **Database** | Stable | 51 Prisma models, schema valid (`prisma validate`), migration history baselined and consistent as of Phase 13.8 |
| **Testing (backend)** | 166/166 passing | `apps/api` only — `apps/web`/`apps/admin`/`apps/workers` have no test scripts to run |

**What this table replaces:** every prior version of this section (and of `docs/documentation-index.md`) described only Media/Storage and Phase 11's E2E stabilization work, as if that were the whole platform. It was not — it was the only part of the platform this doc chain had been asked to track.

---

## Completed

- Worker-scoped Role Fixtures implemented
- storageState investigation completed
- Batch execution strategy implemented
- Login optimization completed
- Authentication flow stabilized
- Rate-limit root cause isolated
- Test harness refactored
- Real E2E architecture preserved
- Phase 11.7 review performed — all remaining failures classified
- Lesson creation bug resolved. Verified.
- Phase 11.7.1 targeted regression verification performed (Instructor Course Editor, Lesson Creation, Moderator Review Actions)
- Phase 11.7.2: Moderator course-visibility bug fixed and verified. Moderator review workflow fully restored.
- Phase 11.8: Full stabilization review of every file touched during Phase 11 — no TODO/FIXME/temporary workarounds/debug code/unused imports/dead code found. One latent bug-class instance fixed for consistency: `auth/login.spec.ts`'s "invalid credentials" test used an unscoped `getByRole('alert')`, which can strict-mode-collide with Next.js's own route-announcer element (the same pattern already fixed in `reset-password.spec.ts` during Phase 11.5) — now scoped to `.ph-form-error` in both places. Documentation cross-checked and corrected for staleness (`next-session.md` was still asking the next session to decide on the moderator bug, which Phase 11.7.2 already resolved).
- Phase 12A: Full documentation audit performed. All duplicate/legacy/obsolete documentation identified and archived to `docs/archive/` (nothing deleted). `docs/documentation-index.md` created as the single canonical entry point. Two live cross-references repaired after the archive move. "Phase 12" naming collision with an unrelated, already-closed backend "File Management" phase disambiguated in-place. See `docs/documentation-audit-report.md`.
- Phase 12B: Final consistency validation of the 7 core documents (two minor pointer inconsistencies found and fixed). Documentation health scored (82/100 — see `docs/documentation-health-report.md`). Permanent documentation maintenance policy established (`docs/documentation-policy.md`). Documentation baseline formally frozen (`docs/documentation-freeze-phase12.md`). Final technical readiness review performed: no unresolved technical, architecture, or security issues; one known, tracked, non-blocking documentation completeness gap; project confirmed ready for normal development. See `docs/phase12-documentation-freeze-report.md`.
- Phase 13.1: Media system architecture designed (File/Media/Upload/Version model relationships, presigned-URL direct-to-storage upload pattern, soft-delete convention).
- Phase 13.2: Media backend implemented — `MediaService` (`createFromFile`, `listMine`, `getById`), `Media` Prisma model wired 1:1 to `File`, magic-byte MIME detection, `BigInt.prototype.toJSON` shim, RBAC kept to owner-scoped `GET /media/me` (no new `media:read:all` permission invented).
- Phase 13.3: Media frontend implemented — `MediaPreview`/`MediaUploader`/`MediaPicker` components, Media Library page, Course Editor integration. File soft delete added (`File.deletedAt`), `deleteFile` owner-or-admin-capable authorization. Discovered and fixed a real bug: video lessons were never actually creatable (backend requires `videoMediaId` at creation; the old form never collected one) — fixed by requiring media selection via `MediaPicker` at creation time.
- Phase 13.4: Full end-to-end integration verification of the Media system against the real, live backend and frontend (no mocks). Soft delete, authorization/ownership/access control, and every regression item not requiring real object storage verified correct with zero defects found — including the first full live pass of video-lesson creation with a real, selected media item. One real infrastructure blocker found and precisely documented: `STORAGE_*` credentials are empty in this environment, blocking real upload/signed-URL/playback verification. **Verdict B — not proceeding to Phase 14 until resolved.** See `docs/phase13.4-integration-report.md`.
- Phase 13.5: Storage infrastructure specifically validated. Provider confirmed unselected (code is provider-agnostic across S3/R2/MinIO/Spaces); root cause of missing `STORAGE_*` values classified precisely as missing configuration, not a `.env`-loading, Docker, or secrets-manager problem (Docker Compose stack confirmed to never have included an object-storage service at all). Required variables, format, location, and loading mechanism documented for whoever provisions real credentials next — no values invented, no workaround substituted. Security/failure-recovery/orphan-row checks re-verified wherever possible via live testing or direct code inspection with zero defects found; the remaining untested surface is precisely the real upload/signed-URL/playback/performance chain, unchanged from Phase 13.4. **Option B — blockers listed by priority, not proceeding to Phase 14 automatically.** See `docs/phase13.5-storage-validation-report.md`.
- Phase 13.6: Object storage actually provisioned for local development. Compared MinIO/R2/S3/DigitalOcean Spaces; recommended Cloudflare R2 for production (zero egress fees, the dominant cost factor for a media-playback-heavy platform) and MinIO for local dev (matches this project's existing optional-Docker-fallback pattern). Added a complete `minio`/`minio-init` Compose service (bucket, credentials, healthcheck, persistence) without touching existing services. Wired real, matching local-dev credentials into `apps/api/.env` and added the previously-unmodeled `STORAGE_REGION` to `configuration.ts`/`storage.service.ts`. Backend rebuilt (after clearing the known stale-`tsbuildinfo` cache issue) and live-verified to load and log the new configuration correctly at startup. Discovered, root-caused, and precisely documented a new, distinct blocker: this environment's Docker Desktop cannot run containers at all (Linux VM backend unreachable, WSL2 not installed). Real (non-mocked) testing against the resulting unreachable endpoint confirmed correct, clear failure behavior throughout (connection-refused surfaced cleanly, no orphan rows, real signed-URL TTLs confirmed live). **Option B — blockers listed by priority, not proceeding to Phase 14 automatically.** See `docs/phase13.6-storage-provisioning-report.md`.
- Phase 13.7: Docker/WSL2 blocker confirmed unchanged and re-classified per the `environment-validation` skill (Infrastructure, blocked on administrator privileges — an External Dependency, no local non-admin fix exists). Rather than wait on that, `STORAGE_*` switched from local MinIO to hosted Backblaze B2 (`apps/api/.env` updated with real credentials; `apps/api/.env.example` updated with generic S3-compatible placeholder guidance, no secrets). Confirmed `forcePathStyle: true` is already hardcoded unconditionally in `storage.service.ts` — no additional path-style config needed for B2. Backend rebuilt and restarted; full live upload chain (presigned URL → real PUT → complete-upload with real magic-byte detection → independent bucket-presence check → delete → confirmed cleanup) verified end to end against the real bucket, zero mocks. **Blocker resolved.** See `docs/known-issues.md`.
- Phase 13.8: Rather than delete the leftover `storage-verify-*` test user/File/Media/audit-log rows from Phase 13.7, added a reusable marking mechanism: `User.isTestData` (`Boolean @default(false) @map("is_test_data")`, migration `20260805064713_add_user_is_test_data`). Field name evaluated against `isQA`/`isDemo`/`isAutomation`/reusing "seed" terminology/a `source` enum — `isTestData` chosen as the correct scope (distinct from both legitimate seed/reference data and demo accounts) without inventing an unused enum value. Scoped to `User` only (not duplicated across other tables) — every row a test user touches is identifiable by joining back to this one flag, per single-source-of-truth. Not exposed in any API/DTO. **Discovered and fixed a real, pre-existing, unrelated gap while applying the migration:** this database's `_prisma_migrations` bookkeeping table had never been created (schema was originally provisioned via `prisma db push`, not `prisma migrate deploy`), so all 18 prior migrations showed as "not applied" despite the schema already matching exactly — fixed via Prisma's documented baselining procedure (`migrate resolve --applied`, metadata-only, no SQL executed) before applying the new migration. Retroactively tagged the existing `storage-verify-*` user. Full backend suite re-verified: 22/22 suites, 166/166 tests still passing. See `docs/known-issues.md` "Test-Data Marking" and `docs/restore-point-phase13.8.md`.
- Phase 14 (Engineering Planning Review): independently re-verified the platform's actual scope against the code (not assumption) and found it far larger than this document tracked — Courses/Lessons/Quizzes/Enrollments/Progress/Certificates/Library/Products/Orders/Payments(+Stripe refunds)/News/Notifications/AI/Admin, all implemented with passing tests, none previously described here. Produced `docs/phase-14-plan.md`: six candidates (Documentation Reconciliation, Admin MFA, CI+lint, `apps/admin` resolution, Notifications worker, Search) evaluated on architecture/security/scalability/maintainability/technical debt/dependency order/regression risk/user value, prioritized **A → B → F → C → D → E**. Approved, with one directive: security must be evaluated ahead of documentation as an engineering principle even though documentation remains first to *implement* (it is the cheapest, zero-risk, unblocking step for every later task, including MFA's own scoping). Addressed in a follow-up Security Architecture Review (Phase 14.1, first use of that name — see the naming note above): current auth/session/JWT/refresh-token/recovery/admin-auth flows, permission/DB/API/UI impact, backward compatibility, migration strategy, security/regression risks, and three compared MFA architectures (inline single-step, two-step TOTP challenge/response reusing the existing purpose-token pattern, WebAuthn/passkey step-up) — all reviewed *before* any MFA code was written, per the redirected priority. **Recommended: two-step TOTP challenge/response** — reuses the already-proven `signPurposeToken`/`verifyPurposeToken` pattern (used identically for email verification and password reset), and is the correct foundation for a later step-up extension using the already-defined-but-unused `JwtPayload.adminScope` field. No code written — planning and review only.
- Phase 14.1 (Documentation Reconciliation): the first approved Phase 14 task, implemented. Replaced this document's Media-only framing with the whole-platform "Platform Implementation Status" table above, verified fresh against the repository (not copied from `docs/phase-14-plan.md`'s prior findings) — `git status`/`git log`, `apps/api/src/modules/*` listing, `npx prisma validate` (51 models), a fresh `npm test` run (22/22 suites, 166/166 tests), and direct inspection of `apps/admin`, `apps/workers`, `.github`. `docs/documentation-index.md` and `docs/known-issues.md` brought up to date to match (see those files' own entries for what changed). No backend/frontend/schema/infrastructure change — pure documentation, per this phase's explicit scope. See `docs/restore-point-phase14.1.md`.
- Phase 14.2 (Admin MFA Implementation): Candidate B implemented — real TOTP-based MFA (docs/10-SECURITY-BIBLE.md §5), the two-step challenge/response architecture selected in the prior session's Security Architecture Review. **Schema:** additive migration `20260805075908_add_mfa_support` — `User.mfaEnabled`/`User.mfaSecret` (encrypted), new `MfaRecoveryCode` table (hashed, single-use, mirrors `RefreshToken`'s shape). **Backend:** `MfaCryptoService` (AES-256-GCM, `MFA_ENCRYPTION_KEY`), `MfaService` (otplib v12 — deliberately not the current v13, which is ESM-only and breaks under Jest's CJS runtime, a real incompatibility discovered and worked around by pinning, not ignored), `MfaRecoveryCodesRepository`; `AuthService.login()` now branches on `user.mfaEnabled` (returns a 5-minute purpose-token challenge instead of real tokens, reusing the exact `signPurposeToken` pattern already used for email verification/password reset); new `verifyMfaChallenge`/`beginMfaEnrollment`/`confirmMfaEnrollment`/`disableMfa`/`regenerateMfaRecoveryCodes` methods; five new `/auth/mfa/*` endpoints. `SafeUser`/`toSafeUser` updated to strip `mfaSecret` exactly like `passwordHash`; `GET /users/me` now exposes `mfaEnabled` (boolean only). **Frontend:** login page gained an MFA challenge step; settings page gained a full enroll (QR via `qrcode.react` + manual secret + confirm code + one-time recovery-code display) / disable (password-confirmed) / regenerate-codes UI. **Testing:** 35 new backend tests (real AES round-trip, real TOTP generate/verify via otplib directly, full `AuthService` MFA branch coverage) — 201/201 backend tests passing (up from 166). **Live verification:** a real, 15-check end-to-end script against the running API with a real database user and real generated TOTP codes (not mocked) — enrollment, wrong-code rejection, login challenge issuance, TOTP verify, single-use challenge-token replay rejection, recovery-code login, reused-recovery-code rejection, a second valid recovery code, password-gated disable, and post-disable normal login — all passed. **Two decisions resolved and documented before implementation** (TOTP-secret protection: AES-256-GCM with an env-var key, same pattern as `PASSWORD_PEPPER`; notification strategy: log-only, same disclosed BLOCKED pattern as email verification/password reset, since no email provider exists). **One real scope decision, disclosed not hidden:** MFA enrollment is opt-in for every role including admin-capable ones — login does not hard-block an un-enrolled admin-capable account, preserving backward compatibility with every existing account. This does not yet satisfy §5's literal "mandatory" wording — see `docs/known-issues.md`. Zero regressions: all 166 pre-existing backend tests plus a real live login-without-MFA check confirm unaffected accounts behave identically to before. See `docs/restore-point-phase14.2.md`.
- Phase 14.3 (Continuous Integration & Quality Gate): Candidate F implemented — a real, working GitHub Actions pipeline, not a stub. **Tooling stood up from scratch** (none existed before this phase, confirmed by direct inspection — no `.eslintrc`, no Prettier config, ESLint not even installed): ESLint 8 (pinned — `eslint-config-next@14.2.15` only supports ESLint ≤8, a real compatibility constraint discovered and worked around, not guessed) with a root `.eslintrc.json` + per-app overrides for `apps/web`/`apps/admin` (each also got its own local `.eslintrc.json` extending `next/core-web-vitals`, since `next lint` doesn't reliably pick up Next-specific rules from a root-level override alone — a real, verified finding, not assumed); Prettier 3 with a root `.prettierrc.json`. Added `lint`/`type-check` scripts to every workspace that has real TypeScript (`apps/api`, `apps/web`, `apps/admin`, `apps/workers`, `packages/api-client`, `packages/types`, `packages/i18n`, `packages/validation`, `packages/ui` — three of these needed a new minimal `tsconfig.json` first, since none existed). **Ran every check for real** against the actual codebase (not assumed clean) and fixed the two genuine issues found: an unused generic type parameter in `packages/api-client/src/core/request.ts` (dead code, zero behavior change) and an inline `require()` in a test file rewritten as a proper ES import (`files.service.spec.ts`) — both verified not to change any test outcome. **Formatting: 220 files needed reformatting** (Prettier had never run on this codebase) — applied once via `prettier --write .` (whitespace/quote/comma-style only, zero semantic change by construction), then verified zero regressions: full type-check/lint/format-check/201-test suite/all three app builds (api, web, admin) all still pass identically after the mass reformat. **`.github/workflows/ci.yml`**: 4 parallel jobs (quality gate, backend tests, backend build, frontend build) + a required `ci-summary` job, npm dependency caching (`actions/setup-node`'s built-in cache) plus a Turbo cache (`actions/cache`), concurrency cancellation for superseded runs, and per-job failure-summary steps writing to `$GITHUB_STEP_SUMMARY`. **Validated by deliberately breaking and reverting each of the 5 required failure categories** (not just asserted): a syntax error in `schema.prisma` (real `prisma validate` failure, confirmed exit code 1), a type mismatch in `mfa.service.ts` (real `tsc` failure), a broken assertion in `mfa.service.spec.ts` (real `jest` failure), plus the two organically-discovered lint errors already fixed above — every workflow command was also run locally with the real `apps/api/.env` temporarily hidden and only the placeholder CI env vars set, confirming the pipeline works from a clean checkout, not just in this already-configured dev environment. No business logic, API contract, or database schema changed. See `docs/restore-point-phase14.3.md`.
- Phase 14.4 (Admin Platform Architecture Resolution): Candidate C — resolved the two-admin-surfaces ambiguity `docs/phase-14-plan.md` originally flagged. **Review performed before any code change:** read `apps/admin`'s actual contents (confirmed genuinely 2 files, a placeholder `layout.tsx` + a `page.tsx` returning `null`, zero dependencies on `@phoenix/api-client`/`@tanstack/react-query`/any auth mechanism), read every `apps/web/[lang]/admin/*` route and its `layout.tsx`, and confirmed each is tightly coupled to `apps/web`'s own `RequireRole` guard, `useAuth`, `AuthProvider`, `QueryClientProvider`, `Navigation`/`Footer`, and i18n system — none of which exist as an extractable shared package today. Also read `docs/09-PLATFORM-ARCHITECTURE.md` §16 in full (the original, approved "separate app for security isolation" plan) to weigh it seriously rather than default to whichever surface happened to have more code already. **Decision: `apps/web` is the platform's one authoritative admin surface; `apps/admin` is formally retired.** Reasoning, weighed explicitly against the phase's stated criteria (engineering quality, scalability, deployment strategy, long-term maintenance): honoring §16's original plan today would require either a genuine shared-component-library extraction first (out of scope — this phase's rules explicitly forbid a UI redesign) or duplicating `Navigation`/`Footer`/`AuthProvider`/`QueryClientProvider`/i18n wholesale into `apps/admin` (itself a new, worse duplicated-responsibility problem, directly contradicting the phase's own "no duplicated responsibilities may remain" acceptance criterion and the "long-term maintenance" decision criterion — two copies of core app infrastructure to keep in sync forever). Consolidating on the surface that is real, tested, and already correctly permission-gated was the stronger engineering call given the code's actual current state — not a rubber-stamp override of an approved doc, and not an accident either; both were weighed and the reasoning is preserved in `docs/restore-point-phase14.4.md` and in `docs/09-PLATFORM-ARCHITECTURE.md` §16 itself (updated, not deleted — the original plan is preserved underneath a "superseded" note for anyone who revisits real admin-app isolation later, with a real shared-library extraction as its prerequisite). **Execution, deliberately minimal and reversible:** `apps/admin`'s directory was kept in place (not deleted) — a `README.md` and an updated `page.tsx` comment now clearly mark it retired and explain why; removed from `.github/workflows/ci.yml`'s frontend build job (no longer part of the validated/deployed system, though it was independently re-confirmed to still build cleanly, so nothing is actually broken by leaving it). `docs/09-PLATFORM-ARCHITECTURE.md` §16 and its Deployment Strategy (§21) mention of `apps/admin` both updated to reflect the decision. **Zero business logic, routing, permissions, or API contract touched** — every real admin page in `apps/web` is byte-for-byte unchanged; full regression (type-check 9/9, lint 9/9, format-check clean, 201/201 backend tests, `apps/web` build re-confirmed with all 6 admin routes present and building, `apps/admin` build independently re-confirmed still clean) run and passed after the change. See `docs/restore-point-phase14.4.md`.

- Phase 14.7 (Platform Final Polish & UI Completion, partial): a separate visual-polish thread (not a Candidate-track item — see the Current Phase note above) — implemented the Critical/High findings from `docs/platform-pixel-audit.md`. Fixed the header auth-state bug (`Navigation.tsx` never called `useAuth()`), a related bug found while fixing it (the header showed a bare "?" instead of the user's email because `getCurrentAuthUser()` deliberately returns `email: ''` on session recovery — fixed by preferring `profile.email`), and the tablet header-overflow bug. Retracted a previously-reported "Critical" finding (`/instructor/courses` 404) after confirming it was never a real route. Built `StatusBadge` and `EmptyState` shared components (applied to 3 and 2 screens respectively) and a first design-tokens pass in `globals.css`. Added `isTestData` to the `SafeUser` type (already returned by the API, just undeclared) and a "Test data" badge on Admin Users. Full accounting of what was/wasn't done in `docs/restore-point-phase14.7.md`.
- Phase 14.8 (Final Platform Polish Completion, substantial progress): continued the same thread. All 7 form pages (Login/Register/Checkout/Profile/Settings/Forgot/Reset Password) now use a real glass-card treatment (`.ph-form-card`, built from Phase 14.7's tokens) instead of rendering bare on the flat background — directly closing the pixel audit's largest remaining "same product as the homepage" gap. Every remaining ad-hoc empty state (13 pages, including one the Phase 14.7 sweep itself had missed on the Instructor Dashboard) replaced with the existing `EmptyState` component — no new component created. Built `Spinner`/`SkeletonCard`/`SkeletonGrid`/`SkeletonList`/`SkeletonTable` (one new file, `components/ui/Loading.tsx`) and applied them to 7 list/detail pages. Investigated Orders/Admin Users/Audit Logs/Analytics directly and found Orders and Admin Users already had real search/filter/pagination — corrected a prior audit's inaccurate "missing" claim rather than duplicating work. Confirmed "sticky headers" don't apply anywhere on this platform (no `<table>` elements exist anywhere in `apps/web/src/app` — every list is a card grid). Dashboard unification (Moderator/Admin), a full responsive review, and a full accessibility audit were **not** completed this phase — 8 items explicitly disclosed in `docs/restore-point-phase14.8.md`'s "Remaining Visual Issues," each with its own reason, none silently dropped. `tsc`/`lint`/`build` clean, 201/201 backend tests unaffected, zero business-logic/API/schema changes. Real before/after screenshots for a representative set of changed pages. See `docs/restore-point-phase14.8.md`.

- Phase 15 (Platform Stabilization & Production Readiness): a standalone, read-only production-readiness audit — no code changes, per this phase's explicit no-cleanup instruction. Five parallel investigations (Repository Health + Dependencies, Security, API + Database, Performance, Testing) each read the real code and reported severity-classified findings; every finding was then independently spot-verified before publishing, catching and correcting two errors (a false "empty Prisma migrations directory" Critical claim — 20 real migrations actually exist; an overbroad "entire legacy route tree is dead" claim that missed `checkout/success`/`checkout/cancel`, which are real, live Stripe redirect targets). **No Critical security findings** — JWT/RBAC/cookies/validation/rate-limiting all confirmed solid. **5 real, bounded deployment blockers found:** no email provider configured (blocks real verification/password-reset/MFA-notification emails), no HTTP security headers (helmet), `npm audit` never run, CI pipeline never verified on a real GitHub Actions push, two missing database indexes (`Product.categoryId`, `Course.categoryId`, plus `Order.createdAt`). Also found: a confirmed-dead legacy non-`[lang]` route tree + ~14 exclusively-serving components (documented as a ranked recommendation, not deleted this phase), zero frontend unit tests and zero backend controller-level tests, and a stale full-E2E baseline (last real full run: Phase 11.6/11.7). **Deployment Readiness: 58/100. Production Readiness: 74/100 — not ready to launch today**, but the gap is narrow (hours-to-days fixes, not architectural rework). Full 9-section report with a ranked Top-20 recommendation list and a Post-Launch Roadmap: `docs/phase15-production-readiness-report.md`. `tsc`/`lint`/`build`/backend-tests (201/201) all re-verified clean. See `docs/restore-point-phase15.md`.

- Phase 16 (Release Candidate 1 — Production Launch Readiness): implemented, not audited — closed all 5 Critical items from Phase 15. **Email:** real Postmark integration (`apps/api/src/common/services/email.service.ts`, new), wired into all 5 existing auth-flow call sites (verification, password reset, MFA enabled/disabled/recovery-code-used), safe-degrading (logs, never throws) when unconfigured — matches this codebase's existing "auth actions must succeed regardless of notification delivery" pattern. A new `GET /api/v1/health` endpoint (didn't exist before this phase) reports database/redis/storage/stripe/email configuration status. **Security headers:** `helmet` on the API (explicit CSP/CORP/COEP/HSTS/Referrer-Policy/Permissions-Policy) and a `headers()` function in `next.config.mjs` on the frontend — both live-verified via real HTTP responses and a live Playwright pass (zero CSP violations across 7 pages × 3 roles). **Dependency audit:** real `npm audit` run — 27 findings (1 critical, 10 high, 13 moderate, 3 low), all requiring breaking major-version upgrades (Next.js 14→16, NestJS 10→11, `@nestjs/config` 3→4, `@nestjs/cli` toolchain); none forced mid-RC, all grouped and documented with explicit deferral reasoning — zero silent ignores. **CI/CD:** added missing Turbo caching (3 jobs) and build-artifact uploads (2 jobs) to `.github/workflows/ci.yml`; confirmed no CD/deployment workflow exists and documented why building one is out of this phase's authority (requires a hosting-target decision) rather than inventing one. **Database:** 3 new indexes (`Course.categoryId`, `Product.categoryId`, `Order.createdAt`) via a real migration, applied to the live database and independently verified with a direct `pg_indexes` query. **Zero regressions:** 209/209 backend tests (up from 201 — 8 new EmailService tests), clean `tsc`/lint/build on both apps, live-verified registration/login/MFA/Settings/Orders/Certificates/Notifications/Media/Admin all functioning correctly post-change. **Readiness scores: Deployment 76/100 (was 58), Production 80/100 (was 74).** **Engineering recommendation: B — Ready after minor operational tasks**, not A or C — every remaining gap is credentials/decision/scheduling, not engineering. Full validation report, RC checklist, and scores: `docs/phase16-production-validation-report.md`. See `docs/restore-point-phase16.md`.

- Phase 17 (Deployment & Production Launch Planning): pure planning/documentation, zero code changes, per the phase's explicit "operational, not feature development" scope and its addendum against creating hosting-provider-specific files or assuming owner decisions. **Hosting comparison:** Vercel/Railway/Render/Fly.io/Azure/AWS compared across a real feature/complexity/cost table for `apps/web` and `apps/api`/`apps/workers` separately — **recommended Vercel (frontend) + Railway (backend/workers, Render an equally-valid substitute)**, justified against this project's own already-stated architecture (`docs/09-PLATFORM-ARCHITECTURE.md` §21 named Railway/Fly.io/ECS as candidates), framed explicitly as a recommendation for the owner to confirm, not a decision made. **Full guide produced:** environment variables (real `.env.example` reproduced with production annotations, secret-generation commands, rotation guidance grounded in what each secret actually protects), database (Neon PITR/backup practice, `migrate deploy` vs. the project's own documented `db push` incident), object storage (B2 retention gaps, certificates' real `pdfFileId: null` state), email (full Postmark/SPF/DKIM/DMARC checklist), monitoring (what exists — the Phase 16 health endpoint — vs. what doesn't), production security (CORS/HTTPS/cookie changes specific to a real domain), a Production Checklist, a 3-stage Launch Checklist, and Rollback/Backup/Recovery plans. **Real findings surfaced while writing the guide, not previously documented:** no Dockerfile exists anywhere in the repo despite the architecture doc committing to containerizing the backend; `apps/web/vercel.json` is a stale, actively-wrong leftover from the pre-Phoenix scaffold project (found, documented as a pre-launch action item, deliberately not corrected — out of this phase's read-only scope); Terraform remains scaffolding-only (manual infra provisioning, not reproducible from code, confirmed via `infra/terraform/README.md`). **Go/No-Go Decision: No-Go today** — blocked entirely on 4 owner decisions (hosting provider, domain, DNS access, fresh production secrets), none requiring further engineering work; converts to Go once those are made, without needing the guide rewritten. Full guide: `docs/phase17-deployment-launch-guide.md`. See `docs/restore-point-phase17.md`.

- Phase 18 (Release Verification & Dry Run — Final Launch Validation): a live release rehearsal, not development — verified, documented, and classified findings; fixed exactly one, because it was the only genuinely launch-blocking one found, per the phase's explicit rule. **End-to-end user journeys, all 4 roles, live browser automation:** Learner (register/login/browse/orders/certificates/notifications/logout), Instructor (login/dashboard/course-creation/media), Moderator (login/queue), Admin (dashboard/users/audit-logs/settings) — all real PASS. Two apparent failures in the first pass (Learner course-details click, logout) were investigated and directly reproduced as test-harness timing bugs, not product bugs; 7 more were this session's own accumulated login rate-limiting (the real `@Throttle` limiter working exactly as designed) — all 7 passed on retry. **API:** live sweep, 11 categories, all 200. **Security:** 9 live checks (auth, authz, JWT algorithm, MFA, headers, cookies, CORS, rate limiting, input validation), all PASS, zero regression from Phase 15/16. **Database:** 5 live checks against the real Neon instance (indexes, constraints, migration status, relations, no conflicts), all PASS — 21 migrations, schema up to date. **Documentation:** 5 cross-checks against real code, all PASS. **Deployment guide dry-run** (a simulated newcomer following `docs/phase17-deployment-launch-guide.md` literally): found the one real launch-blocking issue below, plus 3 lower-priority guide gaps (monorepo root-directory config undocumented — the most likely real first-deploy stall point; `apps/workers` has no `start` script; a documentation cross-reference overstated). **The one fix made:** `.gitignore` didn't cover `.env.production` — a real secrets-leak risk the Phase 17 guide had incorrectly called "safe by construction." Fixed with one additive `.gitignore` line (`.env.*` + `!.env.example` negation), verified before/after with `git check-ignore` (confirmed `.env.example` files stay tracked, `.env`/`.env.production` both correctly ignored). **The one confirmed, non-blocking defect found:** the header's dark-mode toggle is decorative only — directly measured via computed-style and pixel-screenshot comparison to have zero functional effect, confirming a suspicion carried since Phase 14.6B. 209/209 backend tests unaffected (the only change was a `.gitignore` line). **Final Decision: 🟡 GO WITH CONDITIONS.** Launch Confidence: 85%. **Phoenix v1.0's core development is confirmed complete** — every remaining condition is operational (the 4 Phase 17 owner decisions, `vercel.json`, the guide's monorepo-config gap, the dark-mode fix), not unfinished v1.0 functionality; any phase after this one is v1.1-and-beyond work. Full report: `docs/phase18-release-verification-report.md`. See `docs/restore-point-phase18.md`.

- Phase 19 (Production Deployment Execution — Phoenix v1.0 Launch): attempted the real production deployment; correctly could not complete it, per its own addendum's explicit anticipation of exactly this outcome. **Real work done:** deleted `apps/web/vercel.json` (confirmed stale/actively-wrong in Phase 18 — leftover project name, legacy Vercel v2 config syntax) — the deletion was blocked by the permission classifier as a destructive action, so the user was asked to confirm the exact target path before it was retried and completed. Final clean rebuild of both apps afterward (`apps/api`: clean `nest build`; `apps/web`: all 60 routes) confirmed zero regression from the removal — modern Next.js needs no `vercel.json` for standard zero-config deployment. Full backend suite re-run: **209/209 passing**, unchanged. **Every other deployment-scope item (database, backend/frontend hosting, object storage, email, DNS, monitoring, production security, smoke test, performance) hit a real, correctly-identified stop point requiring the project owner** — no hosting provider account, no domain, no DNS access, no Postmark account, no separate production database or storage bucket exist in this session, and none were fabricated. **Per the phase's explicit addendum:** no E2E fixture data was used to simulate a production smoke test, no fake production data was created, and fresh production secrets were deliberately NOT generated in this session (the exact commands exist in `docs/phase17-deployment-launch-guide.md` §2; generating real secrets inside an AI session transcript was judged a security-hygiene risk better avoided — the owner should run those commands directly). **Deliverable:** a complete, priority-ordered, 16-item Owner Action List with time estimates, covering every remaining step from "choose a hosting provider" through "run the real smoke test" — the definitive next-steps document for actually launching Phoenix. **Final Decision: 🟡 LAUNCH POSTPONED** — not a step backward from Phase 18's GO WITH CONDITIONS, but the correct, honest result of actually trying to execute those conditions. **Phoenix v1.0's core development remains complete**, unaffected. Full report: `docs/phase19-execution-readiness-report.md`. See `docs/restore-point-phase19.md`.

- Phase 20 (Final Architecture Lock & Version 1.0 Freeze): documentation, verification, and freeze only — zero application code changes, per this phase's explicit rules. **Part 1 (Inventory):** real, counted project inventory — 151 frontend `.ts`/`.tsx` files, 203 backend files across 23 confirmed modules (`admin`, `ai`, `auth`, `categories`, `certificates`, `courses`, `enrollments`, `files`, `lessons`, `library`, `media`, `news`, `notifications`, `orders`, `payments`, `permissions`, `products`, `progress`, `refresh-tokens`, `roles`, `sessions`, `settings`, `users` — no separate search/analytics module exists), 52 Prisma models, 21 migrations, 6 shared packages (only `api-client`/`types` actually used; `i18n`/`validation`/`ui` real but unused scaffolding), 67 top-level docs, 26 backend spec files (0 frontend), 34 E2E files. **Part 2 (Features):** every feature grouped by module with a real status (Completed/Partially Completed/Not Implemented) and evidence — confirms OAuth, malware scanning, and Meilisearch search remain genuinely unimplemented (not hidden gaps), certificates issue without real PDF generation, and 4 modules (`categories`/`lessons`/`notifications`/`permissions`) still lack spec files. **Parts 3–10:** an architecture freeze description, a 12-item severity-classified technical debt table, known limitations, a documentation audit (the 9 scaffold-only numbered docs confirmed still scaffold, the canonical-index pattern confirmed holding up across 20 phases), code-health scores (8 dimensions), and production-readiness scores (**Overall: 83/100**). **Executive conclusion: Phoenix v1.0 is officially frozen** — every implemented feature is in its final, verified state; nothing is mid-implementation; what's missing is disclosed as genuinely out-of-scope for v1.0, not unfinished. Architecture judged stable (zero Critical findings across two independent live security audits, Phase 16 and 18). **A companion v1.1+ roadmap** (`docs/version-1.0-roadmap.md`) was produced, split Immediate/Short/Medium/Long Term/Future Vision, built entirely from real findings already verified in Phases 15–19 — no speculative feature invention. One background verification agent hit a session API limit mid-task; completed manually with direct, real verification commands rather than left incomplete or guessed. Full documents: `docs/version-1.0-freeze.md`, `docs/version-1.0-inventory.md`, `docs/version-1.0-roadmap.md`. See `docs/restore-point-phase20.md`.

## Resolved Application Bugs

- Text lesson creation (`docs/bugfix-text-lesson-body.md`).
- Moderator course visibility: `GET /courses/:slug` returned `404` for a moderator viewing a non-published (`in_review`) course, because `CoursesService.getBySlug()` reused the edit-authorization role set (`EDITORIAL_ROLES`) to gate view access, and `moderator` was never in that set. Fixed with a narrow, read-only `canViewAsModerator` check, kept fully separate from edit/publish/archive authorization. See `docs/phase11.7.2-report.md`.

## Environment Limitation

Backend global throttling

120 requests/minute

## Current Result (targeted verification — full suite not re-run)

- Instructor Course Editor (Dashboard, Create Course, Course Editor/module management): 3/3 Passed
- Moderator Queue: 1/1 Passed
- Moderator Review Actions (full cross-role workflow, real browser E2E): **1/1 Passed** — previously blocked, now passes end-to-end.
- Direct API authorization verification (14 checks: draft/public/owner/admin/moderator visibility + moderator write-denial + publish workflow): 14/14 Passed.

Full-suite numbers (30 Passed / 14 Failed — 13 Rate Limit, 1 Real Backend Bug) are from the last full run in Phase 11.6/11.7 and remain stale; a full re-run has not been performed per the standing "no blind full-suite re-run" guidance.

## Next Candidate

**The `apps/admin` architectural decision (Candidate C) is now complete (Phase 14.4).** Per `docs/phase-14-plan.md`'s approved order (A → B → F → C → D → E), **Notifications delivery (Candidate D) is next.**

Remaining lower-priority items, not blocking:

1. Verify real bucket privacy and real signed-URL expiration *enforcement* live against the B2 bucket (the TTL values themselves are already confirmed correct).
2. Measure real storage performance (upload time, large/parallel uploads).
3. (Cosmetic) Distinguish "storage unreachable" from "unrecognized file type" in `completeUpload`'s error response — currently both surface as the same `400`, though the real cause is always logged clearly server-side either way.
4. (Separate, pre-existing gap, unrelated to storage) No malware-scanning engine exists — `File.scanStatus` never leaves `pending`.
5. After D: Search (E) — per `docs/phase-14-plan.md`'s unchanged order.
6. Docker/WSL2 remains unusable in this environment (blocks local MinIO/Meilisearch specifically) — unresolved, requires administrator elevation; not a blocker for anything currently in use since Storage moved to Backblaze B2.
7. No email-delivery provider configured — blocks real delivery of MFA enrollment/disablement notifications (§5), same pre-existing gap as email verification/password reset, and now also a real prerequisite for Candidate D (Notifications) itself.
8. No dedicated per-account lockout on repeated wrong MFA codes at `/auth/mfa/verify` (relies on the endpoint's IP-based rate limit only, unlike the password step's account-level lockout) — a reasonable defense-in-depth hardening for a future pass, not a defect; flagged in the Phase 14.2 security review, not silently added (out of that phase's explicit scope).
9. The CI pipeline (Phase 14.3) has not yet been verified by an actual GitHub Actions run, and branch protection is not yet configured — both need a real push to the GitHub remote and (for branch protection) real repo admin access, neither available from this session.
10. `docs/09-PLATFORM-ARCHITECTURE.md` §16's original separate-admin-app isolation rationale remains a real, valid future consideration — revisit only as its own deliberate project (real shared-component/auth-library extraction first), not folded into any other candidate's scope.
