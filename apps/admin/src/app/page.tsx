// Retired (Phase 14.4) — see ../../README.md and docs/restore-point-phase14.4.md.
// This app was never built out beyond this placeholder. The platform's
// real admin surface is apps/web/src/app/[lang]/admin/*.
export default function AdminPlaceholderPage() {
  return null;
}
