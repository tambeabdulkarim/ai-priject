# deployment/vercel/

Placeholder. Once the frontend (`apps/web`) is deployed to Vercel, project-level notes (project name, linked domains, environment-variable group names — never real values) belong here. No config exists yet.

Related: `apps/web/vercel.json` does not exist (the stale, wrong-format leftover from the pre-Phoenix scaffold project was deleted in Phase 19) — modern Next.js needs no such file for a standard Vercel deploy; only create one if a genuinely custom build/routing requirement arises.
