# deployment/monitoring/

Placeholder. Once monitoring is wired up (see `docs/phase21-launch-preparation.md` Section 6 and `docs/deployment-checklist.md`'s Monitoring section), record here: which error-tracking project (e.g. Sentry) is linked to which app, which uptime monitor is watching which URL, and what alert routing is configured. No monitoring is currently configured — only a passive `GET /api/v1/health` endpoint exists in the application itself (see `docs/production-readme.md`'s Health Verification section).
