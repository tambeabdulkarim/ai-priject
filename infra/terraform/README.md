# Terraform — Phoenix Infrastructure

Scaffolding only. Cloud infrastructure (managed Postgres, Redis, object storage, container hosting) is provisioned via Terraform starting in Phase 14 (Production Deployment) per `docs/17-IMPLEMENTATION-ROADMAP.md` and `docs/09-PLATFORM-ARCHITECTURE.md` §21. No resources are defined yet — local development uses `infra/docker/docker-compose.yml` instead.
