# Restore Points — سجل تاريخي / Historical Record

**ملاحظة مهمة حول المسارات في هذه الملفات:**
هذه الملفات محفوظة كما هي (verbatim) من مجلد `docs/` في المستودع الرئيسي لمشروع Phoenix — كسجل تاريخي لما تم إنجازه في كل مرحلة. أي إشارة داخل هذه الملفات إلى مسار يبدأ بـ `docs/` أو `scripts/` أو `apps/` **تشير إلى المستودع الرئيسي (Phoenix repository)، وليس إلى مكان داخل هذه الحزمة** (`Phoenix-Deployment-Package/`). هذه الملفات هي سجل قراءة فقط (read-only historical log) وليست جزءًا من خطوات التنفيذ الفعلية — الخطوات الفعلية موجودة في المجلدات `01` إلى `04` و`06` إلى `09`.

**Important note on paths in these files:**
These files are preserved **verbatim** from the `docs/` folder in the main Phoenix repository — a historical record of what was accomplished in each phase. Any reference inside them to a path starting with `docs/`, `scripts/`, or `apps/` **refers to the main Phoenix repository, not a location inside this package** (`Phoenix-Deployment-Package/`). These files are a read-only historical log, not part of the actual execution steps — the real execution steps live in folders `01` through `04` and `06` through `09`.

## Contents

| File | ما تغطيه / What it covers |
|---|---|
| `restore-point-phase20.md` | Version 1.0 Freeze — الإغلاق الرسمي للنسخة 1.0 |
| `restore-point-phase21.md` | v1.1 Strategic Roadmap — خطة النسخة القادمة |
| `restore-point-phase21-launch-prep.md` | Launch Preparation — تحضير النشر التشغيلي |
| `restore-point-phase21-infra-prep.md` | Infrastructure Templates — قوالب البنية التحتية |
| `restore-point-phase21-smoke-test.md` | Smoke Test & Automation — بناء أدوات الاختبار والأتمتة |

لا تحتاج لقراءة هذه الملفات لتنفيذ النشر — هي فقط للسياق والتوثيق التاريخي.
You do not need to read these files to execute a deployment — they exist only for context and historical documentation.
